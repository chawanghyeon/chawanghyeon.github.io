---
title: "Programmers 영어 끝말잇기"
date: 2020-12-22
draft: true
description: "Programmers 영어 끝말잇기"
tags: [algorithm]
categories: [algorithm]
---

# Answer
```java
import java.util.*;

class Solution {

    public int[] solution(int n, String[] words) {
        int[] answer = {0, 0};
        List<String> list = new ArrayList<String>();
        list.add(words[0]);
        
        for(int i = 1; i < words.length; i++){
            if(list.contains(words[i]) || words[i-1].charAt(words[i-1].length() - 1) != words[i].charAt(0)){
                answer[0] = (i % n) + 1;
                answer[1] = (i / n) + 1;
                return answer;
            }
            list.add(words[i]);
        }
        
        return answer;
    }
    
}
```
